export class RoomSlot {
    /**
     * Defines a room slot.
     * @param {Room} room ...The room of the room slot
     * @param {Slot} slot ...The slot of the room slot
     */
    constructor(room, slot) {
        this.room = room;
        this.slot = slot;
    }
}
